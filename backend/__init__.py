"""
Farkle Backend package.

This package contains:
- Database configuration and session management (`database`)
- SQLAlchemy ORM models for all entities (`models`)
- Pydantic schemas for request and response validation (`schemas`)
- CRUD helper functions encapsulating database operations (`crud`)
- FastAPI application entry point and routes (`main`)

Intended for use as the backend service for the Farkle (Mobile) game.
"""
